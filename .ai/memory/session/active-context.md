# Active Context

- **Current Task**: Use the imported helper harness to keep building the nested `shipyard/` CLI app.
- **Primary Goal**: Keep `.ai/` generic and useful while the product grows under `shipyard/`.
- **Current Focus**:
  1. Preserve the root split between `.ai/` and `shipyard/`.
  2. Keep validation commands and docs aligned with the nested app.
  3. Expand the Shipyard CLI and typed tool surface without leaking work into the harness.
