# Bug Fixing Workflow

This file is a compatibility alias.

Use the canonical plural workflow instead:
- `.ai/workflows/bug-fixes.md`

Keep bug-batch logic in the canonical file so the harness has one source of truth for surgical bug work.
