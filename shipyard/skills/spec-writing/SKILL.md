When writing a feature spec:
- Separate the problem statement from objectives and user stories.
- Write acceptance criteria that a reviewer can validate directly.
- Call out edge cases explicitly instead of burying them in prose.
- Keep out-of-scope items explicit when they affect implementation choices.
