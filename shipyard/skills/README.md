# Runtime Skills

This directory holds Shipyard's built-in runtime-native skills.

Each skill is a folder with:

- `manifest.json`
- `SKILL.md`
- optional `references/`
- optional `tools/`
- optional `validators/`

These skills are product runtime assets, not `.ai/` helper docs.
